Closes #

## What changed

<!-- One or two sentences. -->

## How I verified it

<!-- Tests added or run, and anything checked by hand or in a browser. "Tests pass" is
     a start, not an answer: which tests cover this change? -->

## Checklist

- [ ] `npm test` passes locally
- [ ] Behaviour matches `SPEC.md` and `adr/`, or this PR adds a superseding ADR
- [ ] `test/fixtures/expected.json` unchanged (or changed deliberately, with an ADR)
- [ ] `vendor/` unchanged, or regenerated with `npm run vendor`
- [ ] No new network calls from app code
